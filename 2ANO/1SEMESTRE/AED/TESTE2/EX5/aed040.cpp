#include <stdio.h>
#include <iostream>
#include <list>
#include <algorithm>
using namespace std;

int main() {
  int number;
  cin >> number;
  list<int> numbers;
  for (int i = 0; i < number; i++) {
    int p;
    cin >> p;
    numbers.push_back(p);
  }
  int newst;
  cin >> newst;
  list<int> rep;
  for (int j = 0; j < newst; j++) {
    int repst;
    cin >> repst;
    for (int i = 0; i < repst; i++) {
      
    }
    }
  }